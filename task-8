#include <iostream>
using namespace std;
void bölənlər(int x){
   int a=2;
   for(; a<=x; ++a) {
       if(x%a==0){
           cout << a << endl;
       }
}
}
int main() {
   int x;
   cout << "ədəd daxil edin:";
   cin >> x;
    cout <<  "bölənlər" << endl ;
    bölənlər(x);
    return 0;
}
